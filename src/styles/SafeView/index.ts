import { StyleSheet } from "react-native";
import { primaryColors } from "../Colors";

const safeAreaStyle = StyleSheet.create({
  safeArea: { backgroundColor: primaryColors.white },
});

export default safeAreaStyle;
