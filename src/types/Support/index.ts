export interface SupportMutaion {
    title: string;
    category: string;
  }

  export interface Support {
    id: number;
    title: string;
    category: string;
    userId: null | string | number
}